{
  "name": "wallpaper-gallery-vite",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node generate_thumbnails.js && node generate_sitemap.js && concurrently \"vite --open\" \"node server.js\"",
    "build": "node generate_thumbnails.js && node generate_sitemap.js && vite build",
    "start": "node server.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "exifr": "^7.1.3",
    "sharp": "^0.34.5",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "chokidar": "^3.6.0"
  }
}